# OAuth demo repository fixture

Reserved for the small authentication API, login component, tests, and prepared changes. This directory is not a separate Git repository yet.

The agreed owned paths are `src/api/auth/**` and `src/components/login/**`. The approved `POST /api/oauth` response is `{ token, user }`. Building the example application and its changes belongs to the next milestone.
